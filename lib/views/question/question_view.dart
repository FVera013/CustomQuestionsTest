import 'package:flutter/cupertino.dart';

class QuestionView extends StatelessWidget {
  const QuestionView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Welcome to the questions!'),
    );
  }
}
