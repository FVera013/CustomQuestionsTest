import 'package:flutter/cupertino.dart';

class NavBarNoItem extends StatelessWidget {
  const NavBarNoItem({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 40,
      width: 40,
    );
  }
}
